"use client";

// Reuses the same shared settings page experience used by students.
import SettingsPage from "@/app/student/settings/page";

export default function LecturerSettings() {
  return <SettingsPage />;
}
